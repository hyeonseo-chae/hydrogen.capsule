action (Call) {
  description (CALL을 통하여 app-lauch를 실행하는 action입니다)
  type(Constructor)
  collect {
    input (call) {
      type (CALL)
      min (Optional) max (One)
    }
  }
  output (CALL){
   
    evaluate{
      $expr(call)
    }
  }
}
